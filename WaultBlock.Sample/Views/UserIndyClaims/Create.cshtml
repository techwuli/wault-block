@model WaultBlock.Models.UserIndyClaim

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>UserIndyClaim</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="Issued" /> @Html.DisplayNameFor(model => model.Issued)
                    </label>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="ClaimDefinitionId" class="control-label"></label>
                <select asp-for="ClaimDefinitionId" class ="form-control" asp-items="ViewBag.ClaimDefinitionId"></select>
            </div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class ="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
